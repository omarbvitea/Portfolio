---
const {tags} = Astro.props
---
<div class="flex flex-wrap items-center gap-2">
    {tags.map((tag: String) => {
        return <span class="rounded-lg bg-stone-200 px-2 py-1 text-sm">{tag}</span>
    })}
</div>